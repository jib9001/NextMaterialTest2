//this is how to setup the component

import { makeStyles } from '@material-ui/core/styles';
import Paper from '@material-ui/core/Paper';
import Grid from '@material-ui/core/Grid';
import Typography from '@material-ui/core/Typography'
import PropTypes from 'prop-types'

const useStyles = makeStyles((theme) => ({
  root: {
    flexGrow: 1,
    
  },
  paper: {
    padding: theme.spacing(2),
    textAlign: 'center',
    color: theme.palette.text.secondary,
  },
}));

export default function JumboTextBox(props) {
  const classes = useStyles();
  const { post } = props;

  return (
    <div className={classes.root}>
          <Paper className={classes.paper} elevation={1}>
            <Typography variant="h6" gutterBottom>
                {post.jumboHeader}
            </Typography>
            <Typography variant="subtitle1" gutterBottom>
                {post.jumboBody}
            </Typography>
          </Paper>
    </div>
  );
}

JumboTextBox.propTypes = {
    post: PropTypes.object,
};

-------------------------------------------------------------

import * as PropUnits from '../props/props' //this is the import to the props file

//props file
export const heroUnitOne = {
    title: 'Cincinnati Welcomes YOU',
    description:
      "Our region is fueled by coders, designers, engineers, and tech entrepreneurs. Explore why CINCY IS IT.",
    imgText: 'main image description',
    linkText: 'Connect with an Ambassador',
};

export const jumboTextOne = {
    jumboHeader: 'The Cincinnati region is rich with opportunity, and our tech community is growing exponentially.',
    jumboBody: 'We want you, your talent and your skills to be part of this expansion. Regardless of your experience level, CincyisIT will help you find your place in Cincinnati! We’ll make it easy for you to plug in, plot your next move, learn about ongoing training and stay current with all things local. You’re invited to see why Cincy is IT. This is the next town you’ll be proud to call home!', 
  }

export const imageButtonOne = {
    title: 'Explore Cincy',
    url: './images/Park.jpg',
    width: '100%',
  }
  
export const imageButtonTwo = {
    title: 'Tech Community',
    url: './images/Events.jpg',
    width: '100%',
  }
  
export const imageButtonThree = {
    title: 'Stories & News',
    url: './images/Sports.jpg',
    width: '100%',
  }

export default function UnitProps() {
    
}